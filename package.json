{
  "name": "scheduling-app",
  "private": true,
  "description": "Deploy tooling for the Management App's Cloudflare Worker. Neither app needs any of this at runtime — both are self-contained vanilla JS with no build step (CLAUDE.md §X). This exists only to pin Wrangler so git-connected deploys are reproducible.",
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
